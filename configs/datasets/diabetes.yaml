dataset_id: diabetes
path: data/diabetes.csv
file_type: csv

data:
  continuous_columns: all
  missing_policy: error
  drop_columns: []
  clip_eps: 1.0e-6

copula:
  selection_criterion: bic
  allow_rotations: true
  num_threads: 1
  families:
    - indep
    - gaussian
    - student
    - clayton
    - gumbel
    - frank

outputs:
  artifact_dir: artifacts/libraries/diabetes
  table_dir: reports/tables/diabetes